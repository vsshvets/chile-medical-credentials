{
  "type": "object",
  "properties": {
    "answers": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "question_id": {"type": "string"},
          "answer": {"type": "string", "description": "The answer in English, precise about modality."},
          "resolved": {"type": "string", "enum": ["yes", "no", "partial"]},
          "source_url": {"type": "string"},
          "quote": {"type": "string", "description": "VERBATIM Spanish text from that URL supporting the answer. Empty only if unresolved."},
          "source_date": {"type": "string"},
          "note": {"type": "string"}
        },
        "required": ["question_id", "answer", "resolved", "source_url", "quote", "source_date", "note"]
      }
    },
    "bottom_line": {"type": "string"}
  },
  "required": ["answers", "bottom_line"]
}
